# Cifrado AES-256-GCM
cryptography==44.0.0

# QR code para configuracion de 2FA con app autenticadora (opcional)
# Si no se instala, el setup TOTP muestra instrucciones manuales en lugar del QR
qrcode[pil]>=7.4

# Testing
pytest==8.3.4
