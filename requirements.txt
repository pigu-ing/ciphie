# La única dependencia externa de Ciphie.
# AES (Fernet) no existe en la biblioteca estándar de Python,
# por lo que necesitamos este paquete para el cifrado de secretos.
cryptography==44.0.0

# Testing
pytest==8.3.4
